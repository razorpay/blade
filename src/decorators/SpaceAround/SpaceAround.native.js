import React from 'react';
import styled from 'styled-components';

const StyledSpace = styled.View`
  margin: 20px;
`;

const SpaceAround = (props) => <StyledSpace {...props} />;

export default SpaceAround;
