const unit = 8;

const spacings = {
  xxs: `${unit / 4}px`,
  xs: `${unit / 2}px`,
  s: `${unit}px`,
  m: `${(3 * unit) / 2}px`,
  l: `${unit * 2}px`,
  xl: `${(5 * unit) / 2}px`,
  xxl: `${unit * 3}px`,
  xxxl: `${unit * 4}px`,
};

export default spacings;
