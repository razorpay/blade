---
'@razorpay/blade-svelte': patch
---

feat(blade-svelte): add `countrySelectorMode` prop to `PhoneNumberInput` to render the country selector as a `bottomsheet` (default), `dropdown`, or `auto` (dropdown on desktop, bottom sheet on mobile)
