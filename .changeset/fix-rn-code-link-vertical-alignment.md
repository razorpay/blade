---
"@razorpay/blade": patch
---

fix(rn): vertically align inline `Code` and `Link` with adjacent text on React Native by disabling Android's default font padding (`includeFontPadding: false`). Web rendering is unchanged.
