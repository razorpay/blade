---
"@razorpay/blade": minor
---

feat(DatePicker): auto-select date on click in single mode without Apply button

In single date mode, clicking a date now immediately selects it and closes the picker — no Apply button click required. The Apply/Cancel footer buttons are no longer rendered for single selection type. Range datepicker behavior is unchanged.
