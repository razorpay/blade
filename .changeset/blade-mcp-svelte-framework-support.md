---
'@razorpay/blade-mcp': patch
---

feat(blade-mcp): add svelte framework support

Adds optional `framework` param (`react` | `svelte`, default `react`) to component and general doc tools. Introduces `knowledgebase/svelte/` with Installation, Usage, and Button pilot docs. Returns clear availability errors when svelte component docs are missing.
