---
'@razorpay/blade': patch
---

fix(DonutChart): resolve review comments from native DonutChart PR

Wraps `motionEasing` with `castNativeType` for consistency with other native components, adds tooltip display interaction tests for React Native, and regenerates native snapshots.
