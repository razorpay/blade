import { Message } from './Message';

export const WithSingleSelect = (): React.ReactElement => (
  <Message storyLink="Dropdown/With AutoComplete" />
);

export default {
  title: 'Components/Dropdown/With AutoComplete',
};
