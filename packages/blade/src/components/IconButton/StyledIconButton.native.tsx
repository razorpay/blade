import type { ReactElement } from 'react';
import { Pressable } from 'react-native';

const StyledIconButton = (): ReactElement => (
  <Pressable accessibilityRole="button">{/* todo */}</Pressable>
);

export default StyledIconButton;
