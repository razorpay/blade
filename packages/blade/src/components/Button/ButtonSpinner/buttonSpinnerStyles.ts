import type { CSSObject } from 'styled-components';

const buttonSpinnerStyles: CSSObject = {
  position: 'absolute',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
};

export default buttonSpinnerStyles;
