import type { DotNotationSpacingStringToken } from '~src/_helpers/types';

const bulletPadding: Record<number, DotNotationSpacingStringToken> = {
  1: 'spacing.2',
  2: 'spacing.4',
  3: 'spacing.5',
};

export { bulletPadding };
