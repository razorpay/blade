import { BaseText } from '~components/Typography/BaseText';

// Native does not support alignItems of Text inside a div, instead we need to wrap is in a Text
export { BaseText as StyledAmountWrapper };
