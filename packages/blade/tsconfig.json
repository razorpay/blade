{
  "extends": "../../tsconfig.json",
  "include": ["src"],
  "exclude": ["scripts/migrations/__testfixtures__"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~utils": ["src/utils"],
      "~components/*": ["src/components/*"],
      "~tokens/*": ["src/tokens/*"],
      "~src/*": ["src/*"]
    },
    // In Blade side we are targetting `.web` types
    // We can't add both `.native` & `.web` at once.
    "moduleSuffixes": [".all", ".web", ""]
  }
}
