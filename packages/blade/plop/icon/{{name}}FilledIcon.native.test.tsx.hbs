import {{name}}FilledIcon from '.';
import renderWithTheme from '~utils/testing/renderWithTheme.native';

describe('<{{name}}FilledIcon />', () => {
  it('should render {{name}}FilledIcon', () => {
    const renderTree = renderWithTheme(
      <{{name}}FilledIcon color="feedback.icon.neutral.intense" size="large" />,
    ).toJSON();
    expect(renderTree).toMatchSnapshot();
  });
});
