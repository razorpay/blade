import { IMPORTED_SVG_COMPONENTS } from '../_Svg';
import type { IconComponent } from '..';
import useIconProps from '../useIconProps';
import { assignWithoutSideEffects } from '~utils/assignWithoutSideEffects';

const _{{name}}FilledIcon: IconComponent = ({ size, color, ...styledProps }) => {
  const { height, width, iconColor } = useIconProps({ size, color });

  return (
    REPLACE_SVG
  );
};

const {{name}}FilledIcon = assignWithoutSideEffects(_{{name}}FilledIcon, {
  componentId: '{{name}}FilledIcon',
});

export default {{name}}FilledIcon;
