import { Meta, DocsContainer } from '@storybook/addon-docs';
import { useTheme, BladeProvider } from '@razorpay/blade/components';
import { paymentTheme } from '@razorpay/blade/tokens';

<Meta
  title="Tokens/Typography"
  parameters={{
    docs: {
      container: ({ children, context }) => (
        <DocsContainer context={context}>
          <BladeProvider
            key={`${context.globals.themeTokens}-${context.globals.colorScheme}`}
            themeTokens={paymentTheme}
          >
            {children}
          </BladeProvider>
        </DocsContainer>
      ),
    },
  }}
/>

# 🔡 Typography

export const Typography = () => {
  const { theme } = useTheme();
  return (
    <>
      <table>
        <thead>
          <tr>
            <th>Font Family</th>
            <th>Value</th>
          </tr>
        </thead>
        <tbody>
          {Object.entries(theme.typography.fonts.family).map(([key, value]) => (
            <tr key={key}>
              <td>{key}</td>
              <td>{value}</td>
            </tr>
          ))}
        </tbody>
      </table>
      <table>
        <thead>
          <tr>
            <th>Weight</th>
            <th>Value</th>
          </tr>
        </thead>
        <tbody>
          {Object.entries(theme.typography.fonts.weight).map(([key, value]) => (
            <tr key={key}>
              <td>{key}</td>
              <td>{value}</td>
            </tr>
          ))}
        </tbody>
      </table>
      <table>
        <thead>
          <tr>
            <th>Size</th>
            <th />
          </tr>
        </thead>
        <tbody>
          {Object.entries(theme.typography.fonts.size).map(([key, value]) => (
            <tr key={key}>
              <td>{key}</td>
              <td style={{ fontSize: `${value}px`, padding: '16px' }}>Era of rising Fintech</td>
            </tr>
          ))}
        </tbody>
      </table>
    </>
  );
};

<Typography />
