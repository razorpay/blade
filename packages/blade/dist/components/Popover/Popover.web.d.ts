import { default as React } from 'react';
import { PopoverProps } from './types';
declare const Popover: ({ content, title, titleLeading, footer, children, placement, onOpenChange, zIndex, isOpen, defaultIsOpen, initialFocusRef, ...rest }: PopoverProps) => React.ReactElement;
export { Popover };
