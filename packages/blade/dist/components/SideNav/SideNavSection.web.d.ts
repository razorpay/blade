import { default as React } from 'react';
import { SideNavSectionProps } from './types';
declare const SideNavSection: ({ children, title, defaultIsExpanded, maxVisibleItems, onExpandChange, ...rest }: SideNavSectionProps) => React.ReactElement;
export { SideNavSection };
