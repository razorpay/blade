import { default as React } from 'react';
import { SideNavFooterProps } from './types';
declare const SideNavFooter: ({ children }: SideNavFooterProps) => React.ReactElement;
export { SideNavFooter };
