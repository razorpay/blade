import { default as React } from 'react';
import { SideNavLinkProps } from '../types';
declare const SideNavLink: ({ title, description, href, children, titleSuffix, trailing, isActive, icon, tooltip, as, target, onClick, ...rest }: SideNavLinkProps) => React.ReactElement;
export { SideNavLink };
