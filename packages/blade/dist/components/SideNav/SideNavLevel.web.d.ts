import { default as React } from 'react';
import { SideNavLevelProps } from './types';
declare const SideNavLevel: ({ children, titleSuffix }: SideNavLevelProps) => React.ReactElement;
export { SideNavLevel };
