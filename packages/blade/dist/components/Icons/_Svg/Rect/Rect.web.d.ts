import { ReactElement } from 'react';
import { RectProps } from './types';
declare const Rect: ({ height, width, rx, ry, x, y, fill, fillOpacity, transform, }: RectProps) => ReactElement;
export default Rect;
