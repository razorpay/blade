import { ReactElement } from 'react';
import { SvgProps } from './types';
declare const Svg: ({ children, height, viewBox, width, fill, testID, display, fillOpacity, ...styledProps }: SvgProps) => ReactElement;
export default Svg;
