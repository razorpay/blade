import { ReactElement } from 'react';
import { CircleProps } from './types';
declare const Circle: ({ cx, cy, x, y, r, fill, stroke, strokeWidth, fillOpacity, }: CircleProps) => ReactElement;
export default Circle;
