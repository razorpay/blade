import { CSSObject } from 'styled-components';
import { StyledBaseTextProps } from './types';
declare const getBaseTextStyles: ({ color, fontFamily, fontSize, fontWeight, fontStyle, textDecorationLine, numberOfLines, wordBreak, lineHeight, letterSpacing, textAlign, opacity, theme, textTransform, }: StyledBaseTextProps) => CSSObject;
export default getBaseTextStyles;
