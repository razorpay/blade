import { default as React } from 'react';
import { DropdownOverlayProps } from './types';
declare const DropdownOverlay: ({ children, testID, zIndex, width, minWidth, maxWidth, referenceRef, defaultPlacement, _isNestedDropdown, }: DropdownOverlayProps) => React.ReactElement | null;
export { DropdownOverlay };
