import { default as React } from 'react';
import { TooltipProps } from './types';
declare const Tooltip: ({ title, content, children, placement, onOpenChange, zIndex, ...rest }: TooltipProps) => React.ReactElement;
export { Tooltip };
