import { default as React } from 'react';
import { CircularProgressBarFilledProps } from './types';
declare const CircularProgressBarFilled: ({ progressPercent, fillColor, backgroundColor, size, label, showPercentage, isMeter, }: CircularProgressBarFilledProps) => React.ReactElement;
export { CircularProgressBarFilled };
