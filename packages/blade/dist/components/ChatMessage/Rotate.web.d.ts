import { default as React } from 'react';
declare const Rotate: ({ children, animate, }: {
    children: React.ReactElement;
    animate?: boolean | undefined;
}) => React.ReactElement;
export default Rotate;
