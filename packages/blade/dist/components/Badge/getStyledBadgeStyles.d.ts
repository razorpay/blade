import { CSSObject } from 'styled-components';
import { StyledBadgeProps } from './types';
declare const getStyledBadgeStyles: ({ theme, backgroundColor }: StyledBadgeProps) => CSSObject;
export { getStyledBadgeStyles };
