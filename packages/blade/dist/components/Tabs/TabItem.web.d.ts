import { default as React } from 'react';
import { TabItemProps } from './types';
declare const TabItem: ({ children, value, leading: Leading, trailing, isDisabled, href, onClick, ...rest }: TabItemProps) => React.ReactElement;
export { TabItem };
