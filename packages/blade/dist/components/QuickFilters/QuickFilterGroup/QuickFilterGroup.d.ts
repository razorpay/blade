import { default as React } from 'react';
import { QuickFilterGroupProps } from '../types';
declare const QuickFilterGroup: ({ children, testID, value, defaultValue, onChange, name, selectionType, ...rest }: QuickFilterGroupProps) => React.ReactElement;
export { QuickFilterGroup };
