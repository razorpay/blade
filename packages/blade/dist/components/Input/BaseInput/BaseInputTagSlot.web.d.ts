import { default as React } from 'react';
import { BaseInputTagSlotProps } from './types';
declare const BaseInputTagSlot: ({ renderAs, children, tags, maxTagRows, showAllTags, setShouldIgnoreBlurAnimation, handleOnInputClick, isDropdownTrigger, visibleTagsCountRef, labelPrefix, isDisabled, numberOfLines, isTextArea, size, }: BaseInputTagSlotProps) => React.ReactElement;
export { BaseInputTagSlot };
