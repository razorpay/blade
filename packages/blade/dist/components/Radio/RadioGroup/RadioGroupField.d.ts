import { default as React } from 'react';
type RadioGroupFieldProps = {
    children: React.ReactNode;
    labelledBy: string;
};
declare const RadioGroupField: ({ children, labelledBy }: RadioGroupFieldProps) => React.ReactElement;
export { RadioGroupField };
