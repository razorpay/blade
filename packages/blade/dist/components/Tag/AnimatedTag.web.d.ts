import { default as React } from 'react';
import { AnimatedTagProps } from './types';
declare const AnimatedTag: ({ children, currentTagIndex, isDisabled, activeTagIndex, onDismiss, size, }: AnimatedTagProps) => React.ReactElement;
export { AnimatedTag };
