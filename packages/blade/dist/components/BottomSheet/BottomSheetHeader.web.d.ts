import { default as React } from 'react';
import { BottomSheetHeaderProps } from './types';
declare const BottomSheetHeader: ({ title, subtitle, leading, titleSuffix, trailing, showBackButton, onBackButtonClick, children, ...dataAnalyticsProps }: BottomSheetHeaderProps) => React.ReactElement;
export { BottomSheetHeader };
