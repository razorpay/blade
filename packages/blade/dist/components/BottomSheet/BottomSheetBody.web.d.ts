import { default as React } from 'react';
import { BottomSheetBodyProps } from './types';
declare const BottomSheetBody: ({ children, padding, overflow, ...dataAnalyticsProps }: BottomSheetBodyProps) => React.ReactElement;
export { BottomSheetBody };
