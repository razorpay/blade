import { ReactElement } from 'react';
import { BladeProviderProps } from './types';
declare const BladeProvider: ({ themeTokens, colorScheme: initialColorScheme, children, }: BladeProviderProps) => ReactElement;
export { BladeProvider };
