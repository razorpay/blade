type CheckboxAccessibilityProps = {
    isDisabled?: boolean;
    isChecked?: boolean;
};
export declare const getCheckboxAccessibilityProps: ({ isDisabled, isChecked, }: CheckboxAccessibilityProps) => Record<string, unknown>;
export {};
