/**
 * Skeleton component for the Blade Design System (Svelte).
 *
 * A loading placeholder that mimics the structure of content while it loads.
 * Uses a two-phase CSS animation: fade-in followed by an infinite pulse.
 *
 * @example
 * ```svelte
 * <script>
 *   import { Skeleton } from '@razorpay/blade-svelte/components';
 * </script>
 *
 * <Skeleton width="100%" height="50px" borderRadius="medium" />
 * ```
 */
export { default as Skeleton } from './Skeleton.svelte';
export type { SkeletonProps } from './types';
