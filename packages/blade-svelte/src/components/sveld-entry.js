/**
 * sveld entry for glob-based `.svelte.d.ts` generation.
 * Glob mode discovers all `.svelte` files under this directory; this file exists only to satisfy sveld's entry requirement.
 */
export {};
