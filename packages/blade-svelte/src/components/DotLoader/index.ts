// Internal component — intentionally not re-exported from `src/components/index.ts`
// so it stays out of the public `@razorpay/blade-svelte` surface.
export { default as DotLoader } from './DotLoader.svelte';
export type { DotLoaderProps } from './types';
