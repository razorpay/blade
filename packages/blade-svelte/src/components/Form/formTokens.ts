// Re-export form tokens from blade-core for convenience
export {
  labelTextSize,
  labelOptionalIndicatorTextSize,
  hintTextSize,
  hintIconSize,
  hintMarginTop,
  labelMarginBottom,
  labelWidth,
  labelLeftMarginRight,
} from '@razorpay/blade-core/styles';

export type {
  FormSize,
  LabelPosition,
  NecessityIndicator,
  ValidationState,
} from '@razorpay/blade-core/styles';

