# Discovery Report: {ComponentName}

> Generated by Discovery Agent | Template v1

## Classification

| Field | Value |
|-------|-------|
| **Tier** | {simple / medium / complex} |
| **Prop Count** | {number} |
| **Sub-components** | {number} |
| **Blade Dependencies** | {number} |
| **Has Context** | {yes/no} |
| **Has Portal** | {yes/no} |
| **Has Animation** | {yes/no} |
| **Has Compound API** | {yes/no} |

## Dependencies

<!-- Check each import from ~components/ against blade-svelte/src/components/index.ts -->

| Component | Import Path | Svelte Status | Action |
|-----------|-------------|---------------|--------|
| {Name} | `~components/{Name}` | ✅ migrated / ❌ not migrated | — / migrate first |

## Props

<!-- Extract from types.ts or inline type definition -->

| Prop | Type | Default | Required | Category |
|------|------|---------|----------|----------|
| {name} | {type} | {default or —} | {yes/no} | {visual / behavior / event / a11y} |

## Event Handlers

<!-- List all callback/handler props -->

| Handler | React Type | Web-only Type |
|---------|-----------|---------------|
| {name} | `React.MouseEvent<HTMLElement>` | `MouseEvent` |

## Sub-components

<!-- Only if component has compound API (e.g., Card.Header, Card.Body) -->

| Name | Props Count | Relationship |
|------|------------|--------------|
| {SubName} | {count} | {child / sibling / wrapper} |

## Rendered DOM Structure

<!-- Document expected HTML element tree from React JSX return for key variants.
     This is the source of truth for verify agent DOM parity checks. -->

**Default variant:**
```
<{element} data-blade-component="{name}">
  └── <{inner}> ... </{inner}>
</{element}>
```

**With icon variant (if applicable):**
```
<{element}>
  ├── <span> (icon wrapper)
  │   └── <svg> (icon)
  └── <span> (text)
</{element}>
```

## Stories to Replicate

| # | Story Name | What It Demonstrates |
|---|-----------|---------------------|
| 1 | {name} | {description} |

## React File Inventory

| File | Lines | Purpose |
|------|-------|---------|
| `{Name}.tsx` | {n} | Main component |
| `types.ts` | {n} | Type definitions |
| `{Name}.stories.tsx` | {n} | Storybook stories |
