# Discovery Report: Badge

> Generated by Discovery Agent | Example from real Badge component

## Classification

| Field | Value |
|-------|-------|
| **Tier** | simple |
| **Prop Count** | 6 |
| **Sub-components** | 0 |
| **Blade Dependencies** | 1 (BaseText — already migrated) |
| **Has Context** | no |
| **Has Portal** | no |
| **Has Animation** | no |
| **Has Compound API** | no |

## Dependencies

| Component | Import Path | Svelte Status | Action |
|-----------|-------------|---------------|--------|
| BaseBox | `~components/Box/BaseBox` | ❌ not migrated | use `<div>` workaround |
| Text | `~components/Typography` | ✅ migrated | — |
| BaseText | `~components/Typography/BaseText` | ✅ migrated | — |
| Icons (InfoIcon) | `~components/Icons` | ❌ not migrated | placeholder, prop accepts component |

## Props

| Prop | Type | Default | Required | Category |
|------|------|---------|----------|----------|
| children | `StringChildrenType` | — | yes | content |
| color | `FeedbackColors \| 'primary'` | `'neutral'` | no | visual |
| emphasis | `SubtleOrIntense` | `'subtle'` | no | visual |
| size | `'xsmall' \| 'small' \| 'medium' \| 'large'` | `'medium'` | no | visual |
| icon | `IconComponent` | — | no | visual |
| testID | `string` | — | no | a11y |

## Event Handlers

None — Badge is a display-only component.

## Sub-components

None.

## Rendered DOM Structure

**Default (no icon):**
```
<div data-blade-component="badge">        <!-- outer wrapper (BaseBox in React) -->
  └── <div>                                <!-- StyledBadge -->
      └── <div>                            <!-- content row (BaseBox) -->
          └── <span>                       <!-- BaseText -->
              └── "Label text"
```

**With icon:**
```
<div data-blade-component="badge">
  └── <div>                                <!-- StyledBadge -->
      └── <div>                            <!-- content row -->
          ├── <span>                       <!-- icon wrapper (BaseBox) -->
          │   └── <svg> (Icon)
          └── <span>                       <!-- BaseText -->
              └── "Label text"
```

## Stories to Replicate

| # | Story Name | What It Demonstrates |
|---|-----------|---------------------|
| 1 | Default | Single badge with controls (Playground) |
| 2 | Small Size | All 6 colors × 2 emphasis at small size |
| 3 | Medium Size | All 6 colors × 2 emphasis at medium size |
| 4 | Large Size | All 6 colors × 2 emphasis at large size |
| 5 | With Icon | All 6 colors × 2 emphasis with InfoIcon |

## React File Inventory

| File | Lines | Purpose |
|------|-------|---------|
| `Badge.tsx` | ~140 | Main component with color logic, forwardRef |
| `types.ts` | ~15 | StyledBadgeProps type |
| `Badge.stories.tsx` | ~130 | 5 stories with variant template |
| `StyledBadge.tsx` | ~30 | Styled-component wrapper (not needed in Svelte) |
| `badgeTokens.ts` | ~40 | Size/padding token maps |
